"""
database.py — SQLite CRUD operations module.

Manages the SQLite database connection, table creation/migration,
and all Create, Read, Update, Delete operations for inventory
records and transaction history.
"""
