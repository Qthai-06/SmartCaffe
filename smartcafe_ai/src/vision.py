"""
vision.py — YOLOv8 inference and image processing module.

Handles loading the YOLOv8 model, running object detection on input
images or camera frames, and returning structured detection results
(bounding boxes, class labels, confidence scores, item counts).
"""
