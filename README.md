# SmartCaffe

## Run

Install dependencies first:

```powershell
python -m pip install -r requirements.txt
```

Start the app from the repository root:

```powershell
python -m streamlit run app.py
```

If you prefer running the package entrypoint directly, use:

```powershell
python -m streamlit run smartcafe_ai/app.py
```
